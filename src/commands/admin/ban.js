const Embed = require("../../util/embed");
const Colour = require("../../util/color");
const { prefix } = require("../../config");

module.exports = {
    name: "ban",
    description: "😭 Ban, bye bye",
    aliases: ["ban"],
    usage: `\n${prefix}ban\n`,
    execute(client, msg, args){
        
    }
}